# Evidence — Repo Audit 2026-06-09 12:08:37 CST

## Commands executed

```bash
git status --short --branch
git remote -v
git log --oneline -5 --decorate
git tag --list --sort=-creatordate
PYTHONPATH=src python3 -m pytest -q
```

## Observed repository state before governance edits

```text
## feat/pr3-turbovec-provider...origin/feat/pr3-turbovec-provider
 M PROJECT_STATUS.md
 M docs/adr/ADR-0001-turbovec-semantic-sidecar-positioning.md
```

Remote:

```text
origin git@gitlab.com:hermes-agent-bastet/agent-memory-os.git (fetch)
origin git@gitlab.com:hermes-agent-bastet/agent-memory-os.git (push)
```

Recent commits:

```text
d75ae93 (HEAD -> feat/pr3-turbovec-provider, origin/feat/pr3-turbovec-provider) feat: add turbovec semantic candidate provider
4c2eb2b (tag: v0.1.0-stable, origin/main, origin/HEAD, main) chore: v0.3 Awakening Baseline - Final Authority Sync
246961e feat: publish v0.3 baseline and resonance prototypes
efe66bb docs: record Hermes activation gates
e1ccdb6 Implement dual-track noisy truth retrieval
```

Observed tags:

```text
v0.1.0-stable
```

## Audit conclusions

- `v0.3-awakened` is not an observed Git tag in this working tree.
- The active branch is `feat/pr3-turbovec-provider`, not `main`.
- PR3 turbovec state is documentation/implementation validation on a feature branch, not production activation.
- Existing uncommitted changes pre-dated this governance pass in `PROJECT_STATUS.md` and `docs/adr/ADR-0001-turbovec-semantic-sidecar-positioning.md`.

## Post-edit test result

```bash
PYTHONPATH=src python3 -m pytest -q
```

Result:

```text
......................................................................   [100%]
```

Exit code: `0`.

## Post-edit consistency checks

- `README.md` and `docs/AgentMemoryOS_README.md` now state `v0.1.0-stable` as the observed release tag and treat v0.3 as validation/roadmap.
- `PROJECT_STATUS.md` now distinguishes package/tag/branch evidence from v0.3 validation language.
- Remaining `v0.3-awakened` mentions are negative/blocker/audit statements, not release claims.
