# Assets

- `integrations/` — clickable integration badges used by the root README.
  These are original wordmark tiles (no third-party trademark artwork).
- `logo.*` — the AgentMemoryOS project logo lands here (in production via a
  separate workstream). Once it arrives it will be wired into:
  - the root `README.md` header (GitHub / GitLab)
  - PyPI (`readme` rendering picks it up from the README)
  - the Web UI brand mark (`src/agent_memory_os/web_ui.py`)
