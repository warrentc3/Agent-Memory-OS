# Copy to `.env` for docker compose. All optional.

# Bearer token guarding the Web console API. If unset, the container generates
# one on first run (retrieve it with: docker compose exec amos agent-memory token show).
AGENT_MEMORY_WEB_TOKEN=

# Friendly name shown to peers during memory sync (defaults to a host+home label).
AGENT_MEMORY_NODE_NAME=
